import { HeroAbout } from "@/components/ui/hero-about";

export default function HeroSection() {
    return (
        <section className="w-full m-auto max-w-7xl pb-20">
            <HeroAbout />
        </section>
    )
}